import React from "react";
import style from "./Note.module.css";

function Note() {
  return (
    <section>
      <textarea />
      <button>ok</button>
      <button>not</button>
      <button>clear</button>
    </section>
  );
}

export default Note;
